(()=>{
  let recentLoadingIndex=null;
  const fixtureViewCache=new Map();
  const cacheKey=(matchId,index)=>`${matchId}:${Math.max(0,Number(index)||0)}`;
  const isRecentLoaded=(match,index)=>{const key=currentMatchId==null?null:`${currentMatchId}:${Math.max(0,Number(index)||0)}`;if(key&&fixtureViewCache.has(key))return true;return !!match?.dataLoaded};
  const decorateRecentCards=()=>{const root=document.querySelector('#recentMatches');if(!root)return;const matches=currentView?.recentMatches||[];[...root.children].forEach((card,i)=>{if(!(card instanceof HTMLElement))return;card.classList.remove('recent-data-loading','recent-data-loaded');card.querySelector('.recent-data-state')?.remove();let state='';if(recentLoadingIndex===i){state='loading';card.classList.add('recent-data-loading')}else if(isRecentLoaded(matches[i],i)){state='loaded';card.classList.add('recent-data-loaded')}if(state){const el=document.createElement('span');el.className='recent-data-state';el.setAttribute('aria-label',state==='loaded'?'Veriler alındı':'Veriler yükleniyor');el.title=state==='loaded'?'Maç verileri alındı':'Maç verileri yükleniyor';el.innerHTML=state==='loaded'?'✓':'<i></i>';card.appendChild(el)}})};
  const decorateFixtureCards=()=>{document.querySelectorAll('#fixturesList .fixture-card').forEach(card=>{card.classList.remove('recent-data-loaded','recent-data-loading');card.querySelector('.recent-data-state')?.remove();const onclick=card.getAttribute('onclick')||'';const match=onclick.match(/selectFixture\((\d+)\)/);const id=match?Number(match[1]):null;if(id!==null&&fixtureViewCache.has(cacheKey(id,selectedRecentIndex))){card.classList.add('recent-data-loaded');const el=document.createElement('span');el.className='recent-data-state';el.setAttribute('aria-label','Maç verileri alındı');el.title='Maç verileri alındı';el.textContent='✓';card.appendChild(el)}})};
  const showCacheInfo=view=>{if(!view?.cache)return;const bar=document.querySelector('#recentSelection');if(!bar)return;const c=view.cache;const source=c.history==='POSTGRES_CACHE'?'PostgreSQL önbellek':(c.history||'CHPP');const lineup=c.lineup==='POSTGRES_CACHE'?'DB kadro':(c.lineup||'CHPP');const analysis=c.analysis==='COMPUTED_AND_CACHED'?'DB HO sonucu':(c.analysis||'HO hesaplandı');bar.dataset.cacheInfo=`${source} • ${lineup} • ${analysis}`;bar.querySelector('.cache-status')?.remove();bar.insertAdjacentHTML('beforeend',`<small class="cache-status">${source} • ${lineup} • ${analysis}</small>`)};
  const originalLoadSelectedMatch=window.loadSelectedMatch;
  window.loadSelectedMatch=async function(index=selectedRecentIndex||0,forceRefresh=false){const normalized=Math.max(0,Number(index)||0);const key=cacheKey(currentMatchId,normalized);recentLoadingIndex=normalized;decorateRecentCards();if(!forceRefresh&&fixtureViewCache.has(key)){currentView=fixtureViewCache.get(key);selectedRecentIndex=Number(currentView.selectedRecentIndex??normalized);recentLoadingIndex=null;renderMatch(currentView);showCacheInfo(currentView);decorateRecentCards();decorateFixtureCards();return}await originalLoadSelectedMatch(normalized);if(currentView?.fixture?.matchId===currentMatchId&&Number(currentView.selectedRecentIndex??normalized)===normalized){fixtureViewCache.set(key,currentView);showCacheInfo(currentView)}recentLoadingIndex=null;decorateRecentCards();decorateFixtureCards()};
  const originalRenderRecent=window.renderRecent;if(typeof originalRenderRecent==='function')window.renderRecent=function(matches){originalRenderRecent(matches);decorateRecentCards()};
  window.selectRecent=async function(i){const previousView=currentView;const previousIndex=selectedRecentIndex;selectedRecentIndex=i;recentLoadingIndex=i;if(previousView?.recentMatches)renderRecent(previousView.recentMatches);decorateRecentCards();await window.loadSelectedMatch(i);if(previousView&&currentView===previousView&&previousIndex!==i){selectedRecentIndex=previousIndex;recentLoadingIndex=null;renderRecent(previousView.recentMatches||[]);const bar=document.querySelector('#recentSelection');if(bar)bar.innerHTML='<b>Maç verisi alınamadı.</b> Mevcut maç bilgileri korunuyor.';const explanation=document.querySelector('#explanation');if(explanation)explanation.textContent='Rakip maç verisi alınamadı. Mevcut analiz korunuyor.'}decorateRecentCards();decorateFixtureCards()};
  const recentRefresh=document.querySelector('#recent .icon-btn');if(recentRefresh)recentRefresh.setAttribute('onclick','loadSelectedMatch(undefined,true)');
  const hideDuplicateChooser=()=>{document.getElementById('cupFormationChooser')?.remove()};
  const isCupMode=()=>document.querySelector('#ownLineupMode button[data-mode="cup"]')?.classList.contains('active');
  document.addEventListener('click',e=>{const option=e.target?.closest?.('#ownFormationMenu .own-formation-option');if(!option||!isCupMode())return;e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();const form=option.dataset.formation;if(!form)return;window.__selectedCupFormation=form;const btn=document.getElementById('ownFormation');if(btn)btn.textContent=form;document.querySelectorAll('#ownFormationMenu .own-formation-option').forEach(x=>x.classList.toggle('selected',x===option));hideDuplicateChooser();setTimeout(()=>{if(typeof window.__renderRecommendedCup==='function')window.__renderRecommendedCup()},0)},true);
  const cleanup=()=>{hideDuplicateChooser();if(isCupMode()){const f=window.__selectedCupFormation||document.getElementById('ownFormation')?.textContent?.trim();if(f&&/^(4-4-2|4-3-3|3-5-2|4-5-1|5-4-1|5-3-2|3-4-3)$/.test(f))window.__selectedCupFormation=f}};
  new MutationObserver(cleanup).observe(document.documentElement,{childList:true,subtree:true});
  setTimeout(cleanup,0);setTimeout(cleanup,500);setTimeout(cleanup,1500);
})();