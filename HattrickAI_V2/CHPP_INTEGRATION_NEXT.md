This branch is reserved for restoring the V1 CHPP OAuth endpoints in V2 without changing the V2 sync engine. Implement endpoints /auth/chpp/start, /auth/chpp/callback, /auth/chpp/logout and /api/status using the existing CHPP settings/session implementation, then wire the V2 UI button to /auth/chpp/start. Do not expose diagnostics or analysis requests before /api/status returns connected=true.